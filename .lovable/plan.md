

# 副标题下调 + 机械布局页面精简

## 问题

1. **副标题仍偏上** — `y: 0.48` 需要继续下调
2. **机械布局页面内容过多** — 右侧有"光学配置"表格（相机、镜头、光源、工控机）不需要，只保留布局说明和基本布局信息

## 修改方案

### 1. 副标题 y 值再下调

所有 `y: 0.48` → `y: 0.52`（6处）：

- **`src/services/pptxGenerator.ts`**：第 741、833、1082 行
- **`src/services/pptx/workstationSlides.ts`**：第 131、135、141 行

### 2. 机械布局页面精简（`src/services/pptx/workstationSlides.ts` 第 644-840 行）

删除右上角光学配置区域（第 744-790 行），包括：
- "光学配置" 标题
- 相机表格 (cameraRows)
- 镜头/光源/工控机表格 (opticsRows)

调整后右侧布局：
- **辅视图**：保留，位置从 `y: 0.7` 下调到 `y: 0.85`，增大高度到 2.8"
- **布局说明**：保留，位置上移到辅视图下方（约 `y: 3.7`）
- **布局信息表**（安装方式/执行机构/相机数量/工作距离/视场）：保留，紧跟布局说明下方

主视图也下调：`y: 0.7` → `y: 0.85`，高度保持 4.2"

这样右侧从上到下只有：辅视图 → 布局说明 → 布局信息，没有光学配置。

