

# 重新设计光学方案图 - 严格匹配上传图片样式

## 当前问题

现有 `VisionSystemDiagram.tsx` 的视觉样式与上传参考图差异较大：
- 当前：光源是带红色LED块的矩形，相机支架不明显，FOV锥体较淡
- 参考图：清晰的相机支架（灰色十字形）、光源条带红色LED带、实心紫色FOV锥体、蓝色虚线标注

## 方案

重写 `VisionSystemDiagram.tsx` 中 SVG 的主体绘制部分（L300-590），严格对照参考图的布局和样式：

### 布局结构（从上到下，居中排列）

```text
┌─────────────┐
│   Cam1      │  ← 紫色相机体（圆角矩形）
└──────┬──────┘
       │         ← 黑色连接柱（镜头接口）
  ┌────┴────┐
  │  镜头   │   ← 深色镜头体（梯形/圆柱）
  └────┬────┘
       │
 ══════╤══════  ← 灰色光源条，两端红色LED带
       │
 ──────┼──────  ← 灰色十字形相机支架/安装板
       │
      / \       ← 紫色半透明FOV锥体
     /   \
    /     \
   /  ●    \    ← 检测点标记（蓝色圆点+引线）
  ┌────────┐
  │  产品  │    ← 灰色产品矩形
  └────────┘

← 165±20mm →    ← 左侧蓝色虚线标注（从光源到产品）
```

### 视觉细节对照参考图

1. **相机体**：紫色圆角矩形，内部显示 "Cam1" 白色文字，顶部有小绿色指示灯
2. **镜头连接**：深灰/黑色短柱连接相机和镜头
3. **镜头体**：深棕色矩形，底部有暗色环带
4. **光源条**：灰色长条，两端各一个红色LED区域（与参考图一致）
5. **相机支架**：灰色十字形安装板（水平长条 + 短竖条），位于光源下方
6. **FOV锥体**：从镜头底部到产品的**实心紫色半透明**三角形（比当前更饱和）
7. **产品**：灰色矩形，下方标注"产品"
8. **检测标记**：产品表面蓝色圆点 + 水平引线
9. **标注线**：左侧蓝色虚线 + 箭头标注工作距离 "165±20mm"
10. **外框**：蓝色虚线边框包围整个光学系统

### 关键改动

- 重写 SVG 主体（相机、镜头、光源、支架、FOV、产品的形状和位置）
- 新增相机支架十字形元素
- FOV 锥体改为更饱和的紫色填充
- 光源条两端红色LED改为条带状（非块状）
- 新增蓝色虚线外框
- 产品上新增检测点标记（蓝色圆点+引线）
- 保留所有交互功能（HardwareSelectPopover）和右侧参数面板不变

## 改动范围

- `src/components/canvas/VisionSystemDiagram.tsx`：重写 SVG 主体绘制（约 L300-590），保留 defs、foreignObject 交互层和右侧注释面板

预计约 200 行改动，1 个文件。

